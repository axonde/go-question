import 'package:flutter/material.dart';

class SettingsUiConstants {
  static const cardBackground = Color(0xFF0E3457);
  static const cardBorder = Color(0xFF5EA3D3);
  static const cardShadowAlpha = 0.45;
  static const tileBackground = Color(0x0FFFFFFF);
  static const tileBorder = Color(0x14FFFFFF);
  static const inactiveTrack = Color(0x1FFFFFFF);

  const SettingsUiConstants._();
}
